[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
defmt = { workspace = true, optional = true }

[dev-dependencies]
crc = { workspace = true }

[features]
default = []
defmt = ["dep:defmt"]
